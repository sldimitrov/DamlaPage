import SectionTemplate from "../Home/SectionTemplate";

export default function Opinions() {
  return (
    <SectionTemplate>
      <section className="h-96 section-title">
        <h1 className="text-black text-center">Мнения</h1>
      </section>
    </SectionTemplate>
  );
}
